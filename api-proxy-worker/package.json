{
  "name": "ghost-subdir-i18n-api-proxy",
  "version": "1.0.0",
  "description": "API proxy Worker for the Ghost i18n system — relays Content API lookups to escape same-zone Worker→Worker restrictions.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260525.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.94.0"
  }
}
