# Secrets — NEVER commit. Set via `wrangler secret put` in production.
.dev.vars
*.env
.env*

# Dependencies — restored via `npm install`.
node_modules/

# Wrangler local state and build artifacts.
.wrangler/
dist/
*.tsbuildinfo

# Editor / OS noise.
.DS_Store
*.swp
.vscode/
.idea/
