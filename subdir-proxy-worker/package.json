{
  "name": "ghost-subdir-proxy",
  "version": "1.0.0",
  "description": "Route adapter Worker: forwards your-blog.com/en/* to the subdir-fetcher-worker over HTTPS.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260525.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.94.0"
  }
}
